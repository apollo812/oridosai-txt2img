version: "3.7"

services:
  unit:
    build:
      context: ..
      dockerfile: Dockerfile
    volumes:
      - ../app:/build/app

  integration:
    build:
      context: ..
      dockerfile: Dockerfile
    volumes:
      - ../app:/build/app
